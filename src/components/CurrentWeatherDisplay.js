import React from 'react';
const CurrentWeatherDisplay = (props) => {
	const { weather } = props;

	return (
		<div>
			<h1>Current Weather Display</h1>
		</div>
	);
};

export default CurrentWeatherDisplay;
